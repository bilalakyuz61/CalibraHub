bin/
obj/
.idea/
.vs/
.dotnet/
.templateengine/

*.user
*.suo
*.nupkg

# Build artifacts & logs
.tmp_build_out/
artifacts/
tmp/
*.log
*.err
*.out
run_output.txt

# Installer output
install/
*.exe

# Installer artifact'leri (kaynak script'ler tracked: CalibraHub.iss, *.ps1)
installer/output/
installer/publish/
installer/grafana/
installer/dependencies/
installer/whatsapp/node_modules/
installer/build-output.log

# Node
node_modules/

# Tools
tools/

# App logs
app.log

# ASP.NET Core Data Protection keys (local, not safe to commit)
.app-data-protection/

# ── Scratch / dev hijyeni ───────────────────────────────────────────────
# Windows path leak (yanlis path quoting sonucu olusan dosyalar)
C:*
# Test session cookies (security: kullanici cookie'sini repo'ya koyma)
_cookies.txt
.test-cookies.txt
# Geçici sayfa render / debug HTML
_pg_render*.html
combo-screen.html
# DOCX/PDF extract gibi geçici klasörler
.tmp_*
# Hangi run prefixi ile baslarsa baslasin tum log dosyalari
.run-*.log
.web-*.log
.claude-*.log
calibra-run.*
server_*.log
sln-build*.log
build*.log
run*.log
worker-*.log
worker.err.log
bridge.*.log
# Build verification ciktilari (PowerShell hookundan)
build-*.log
web-*.log

# ── Build output / payload ──────────────────────────────────────────────
# Publish ciktilari (Web/Worker/WhatsAppBridge) — installer payload kaynagi.
# Yerel build sonuclari, repo'ya commit edilmez; .exe + dll'ler her build'de yeniden uretilir.
publish/

# Grafana — 2.1.x serisinde projeden cikarildi (Rapor Tasarimcisi yerini aldi).
# Mevcut .grafana/ dizinindeki conf+data dosyalari runtime artifact, koklenir.
.grafana/

# ── Geliştirici-spesifik dev artifact'ler (run-out, build-out, vs.) ────
run-out.txt
run-err.txt
worker-out.txt
worker-err.txt
_run*.txt
_run_*.txt
build_out.txt
.docx_extract.txt
.extract_docx.ps1
.run-log.txt
