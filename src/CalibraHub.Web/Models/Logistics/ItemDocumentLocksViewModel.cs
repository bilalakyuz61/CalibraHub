namespace CalibraHub.Web.Models.Logistics;

/// <summary>Malzeme Belge Kilitleri ekranı — CalibraSmartBoard inline config (anonymous object, JSON serialize edilir).</summary>
public sealed class ItemDocumentLocksViewModel
{
    public object? BoardConfig { get; init; }
}
