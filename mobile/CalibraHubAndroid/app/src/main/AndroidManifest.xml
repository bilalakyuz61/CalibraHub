<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WorkManager arka plan + bildirim için (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Barkod tarama (ZXing CaptureActivity kamerayı kullanır; embedded kütüphane kendi
         manifest'inde de deklare eder ama açık deklarasyon burada da şart) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- WhatsApp Business app intent kuyruğu (Android 11+ paket görünürlüğü) -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="wa.me" />
        </intent>
    </queries>

    <application
        android:name=".CalibraApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalibraHub"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="34"
        xmlns:tools="http://schemas.android.com/tools">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CalibraHub"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Barkod tarama — ZXing'in yatay-kilitli varsayılan CaptureActivity'sinin yerine
             geçer (fullSensor: dikey + yatay serbest). Tema/windowSoftInputMode kütüphanenin
             kendi CaptureActivity deklarasyonuyla aynı tutuldu. bkz. FlexibleCaptureActivity.kt,
             MaterialPickerField.barcodeScanOptions(). -->
        <activity
            android:name=".FlexibleCaptureActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden" />

    </application>

</manifest>
